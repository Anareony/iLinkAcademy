import React from 'react'

import { Button } from './styles'

const Button1: React.FC = (props) => {
    return (
        <></>
        // <Button {...props}>
        //     {props.children}
        // </Button>
    )
}

export default Button1;